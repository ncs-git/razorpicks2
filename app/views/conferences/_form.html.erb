<%= simple_form_for(@conference) do |f| %>
  <%= f.error_notification %>

  <div class="form-inputs">
    <%= f.input :name %>
    <%= f.input :nickname %>
  </div>

  <div class="form-actions">
    <%= f.button :submit %>
  </div>
<% end %>
